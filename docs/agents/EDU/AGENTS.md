# Agente EDU — capacidades

## Tools

- `pubmedBusca(query, max=20, since_days=30)`
- `sumarizarArtigo(pmid)` — TL;DR + bullets + nível evidência
- `gerarFlashcardsAnki(topico, n=5)` — APKG
- `monitorarCongressos(area)`
- `pergunta(query)` — Q&A com fontes
- `newsletter_semanal()`
